# 目標：腦啟發、樣本高效、工程可落地的 LLM 持續學習

這份檔案是**現在的最高目標**（north star）。`report.md` 是 CIFAR/pi 階段的完整研究日誌
（那是 proxy，已收斂出下面的約束），`NEXT_STEPS.md` 是 CIFAR backlog。LLM 方向以本檔為準。

---

## 0. 目標（一句話）

打造一個**工程可落地、能用**的 LLM 持續學習系統，達到**大腦級的樣本效率**：
**看幾次就學會、學新不大幅忘舊、不需要海量重訓。** 不是為了發論文。

## 1. 核心 reframe（謎題問對了答案就出來）

- **LLM 其實已經是 one-shot 學習器**：in-context learning 本身就是看一次就會。
- 「需要海量資料才記住」**只發生在硬把知識寫進權重時**——那是「慢速分布式系統
  （對 transformer 做 SGD）做寫入」的成本，不是學習本身的成本。
- **真正的差距是：持久化（durable，不靠海量重訓）+ 非侵蝕（不忘舊）。**
- 大腦省，是因為它**不用慢系統做 one-shot**：快速寫入 + 內部自我回放把「重複」變成
  **內生**的（睡眠回放把一次經驗變上千次鞏固），外部只看幾次。

**原則：別想把 SGD 變成 one-shot。匹配大腦的分工。**

## 2. 四個機制（都可落地，對應大腦）

1. **快速一次寫入（海馬迴）**：檢索 / KV 記憶（Memorizing-Transformer、kNN-LM）+
   模型編輯（ROME/MEMIT）→ 字面 one-shot、零干擾。
2. **內部自我回放（睡眠）**：從 1 條真實曝光，LLM **自我生成 N 個（經驗證的）改寫/推論/
   QA 變體** → 鞏固。把 1 次外部曝光變成多次內部鞏固事件。外部 sample-efficiency=1，
   重複自生。**這是匹配大腦效率的核心。**
3. **schema 綁定（吃預訓）**：鞏固成**凍結 base 上的小 delta**（LoRA / 定點編輯）；base
   已「幾乎知道」→ few-shot 即可（CIFAR 代理證據 P13c：預訓 0.440 ≫ 從零 0.237）。
4. **顯著性門控**：驚訝（高 loss）/ 效用決定寫多重、要不要鞏固、留檢索還是忘掉。
   **遺忘是 feature。**

## 3. Headline 指標（直接對標「人看幾次就會」）

- **達到目標「持久記憶強度」所需的真實曝光次數**（越少越好）。
- **同時付出的通用能力侵蝕**（held-out benchmark 掉多少，越小越好）。
- 目標曲線：**1–3 次真實曝光 + 自我回放 ≈ 多次重訓的記憶強度，且侵蝕有界。**

## 4. 第一個 PoC（正在做）

迴路：**一次曝光 → 存進記憶 → 驗證 → 自我生成 N 變體 → 門控後鞏固進 LoRA（base 凍結）**。
- 基質：小 instruct LLM（先 ~0.5–1.5B）on RTX 2070（8GB）。
- 為什麼直接上 LLM：CIFAR 代理沒有 ICL / 自我改寫 / schema，無法忠實表示這個目標。
- 先量「曝光次數 → 記憶強度」與「侵蝕」兩條曲線,把大腦的 sample efficiency 工程化。

### PoC 量測設計（草案）
- **記憶強度**：對一組「新事實 / 新偏好 / 新格式」探針集,問模型(不給 context)能否正確回答
  → 準確率隨「真實曝光次數 × 是否自我回放 × 鞏固方式」變化。
- **基準對照**：(i) 純檢索(0 次權重寫入,看記得住但不整合);(ii) 純 LoRA 多次重訓(看要幾次);
  (iii) 1 次曝光 + 自我回放鞏固(目標方法)。
- **侵蝕**：固定一組通用能力探針(常識/推理小集),每次鞏固後回測,量掉多少。

## 5. 誠實的硬處（不假裝解了）

- **非侵蝕的鞏固到權重** = 真正的核(20%)：自我回放 → adapter,讓新技能單調增長、舊能力
  侵蝕有界。對應 CIFAR 的表徵級 stability–plasticity 張力(P13c)。
- **自我回放會 confabulate**：種子經驗在自我回放前**必須先驗證/接地**,否則放大幻覺。
- **模型編輯脆弱**：對事實有效,對程序性知識脆弱,多次編輯累積不穩定 + ripple effect。
- 判斷「契合 schema 的快學 vs 違反 schema 的慢學」本身不簡單。

## 7. PoC 結果（進行中）

**Step 1（`llm_cl/probe_oneshot.py`）— reframe 被數字證實**：Qwen2.5-0.5B 在 16 條全新事實上,
durable recall（不給 context）**0.00**、in-context one-shot **0.94**。模型本來就 one-shot 會,
缺的純粹是持久化。

**Step 2（`llm_cl/consolidate.py`）— self-replay 是引擎,naive 重複不行**：把 1 次曝光鞏固進
LoRA（base 凍結),durable recall 用 **held-out 問句**測（不是背問句）,同時量通用能力侵蝕。

| 方法 | epochs | durable recall | 通用能力（侵蝕） |
| :-- | :--: | :--: | :--: |
| naive（只訓陳述句,重複) | 10 / 30 / 60 | 0.50 / 0.50 / 0.50 | 0.75 / 0.83 / 0.83 |
| **self-replay（8 驗證變體)** | 3 / 10 / 20 | 0.75 / 0.62 / **0.88** | **0.92 / 0.92 / 0.92** |

- **naive 重複封頂在 durable 0.50**（10/30/60 epochs 都一樣)——再怎麼重複同一句,事實也只有
  一半進得了「能用」的權重記憶,且**永久侵蝕通用能力 0.17–0.25**。
- **self-replay 兩軸全勝**：durable 衝到 0.75–0.88,且通用能力只掉 0.08（侵蝕 ~1/3 於 naive)。
  **1 次真實曝光 + 自我生成驗證變體,就把「看一次就durable記住、且少忘」工程化了**——正是大腦
  海馬迴一次寫入 + 睡眠回放的機制。
- **caveat**：變體是隨機生成,run-to-run 有變異（epochs=10 那次掉到 0.62）。下一步要 **快取變體 +
  多 seed** 把曲線做乾淨,並掃 variants 數量。

**機制驗證對應**：這直接證實了 §2 的機制 2（內部自我回放)與 4（驗證/接地防 confabulate);
naive vs replay 的侵蝕差也再現了 §2 機制 3（在凍結 base 上小 delta、自然分布資料少侵蝕)。

**Step 3（`llm_cl/stream.py`）— 序列流下的災難性遺忘:rehearsal 必要、但純權重鞏固難**：
24 條事實分 6 批依序進來,LoRA 持續鞏固,每批後回測所有看過批次。

| mode | final avg recall | per-batch final | general |
| :-- | :--: | :-- | :--: |
| naive | 0.04 | [0.25,0,0,0,0,0] | 0.83 |
| replay | 0.04 | [0,0,0,0.25,0,0] | 0.75 |
| **rehearsal** | **0.25** | [0.5,0.25,0,0.25,0,0.5] | 0.83 |

- **naive/replay 在序列流下災難性崩潰**（final 0.04）,而且**連可塑性都垮**（naive new-batch
  recall 0.75→0.00、adapter 飽和）。**關鍵教訓:step 2 的英雄 self-replay 解的是「樣本效率/學新」,
  不是「retention」**——序列流下不回放舊的,照樣全忘。**樣本效率 ≠ 抗遺忘,是兩個問題。**
- **交錯 rehearsal（CLS 睡眠機制)是抗遺忘的必要件,且 ~6× 改善**（0.04→0.25,b0 留住 0.50 vs 0,
  可塑性也較保得住)。方向確認:**讓遺忘停下來的是「回放舊的」(交錯鞏固),正如大腦、也正如 CIFAR
  的 replay 結論。**
- **但遠未解決**：final 0.25 仍低、forgetting +0.40 仍高。限制:0.5B 小模型 + 小 LoRA + 固定
  rehearse_k=12(不隨舊集成長)。這些是調參/容量旋鈕,非根本。

**重大工程啟示**：純粹把事實「塞進權重」的序列鞏固很難（會忘）。大腦與可落地的答案都是**多靠
快速/情節記憶(檢索,零遺忘)、只選擇性鞏固反覆且重要的少數**——所以下一步的高價值在**檢索層
(海馬迴)+ 門控選擇性鞏固**,而不是硬把全部壓進 LoRA。retrieval 立即零遺忘,consolidation 是
昂貴且難的「編譯進權重」,該留給值得的少數。

**Step 4（`llm_cl/memory.py` `retrieval_system.py` `system.py`）— 端到端零遺忘系統 + 門控鞏固**：

(4a) **檢索層（海馬迴）**：事實寫入情節記憶（不動權重）,RAG 回答。24 事實 6 批:
**retrieval recall 整條流持平 1.00（final 0.96)、最舊批次始終 1.00（零遺忘)、通用能力
1.00→1.00（零侵蝕)**。對照 step 3 LoRA 鞏固崩到 0.04——**第一個端到端零遺忘、可用的版本。**

(4b) **完整系統 = 檢索（全部、零遺忘) + 門控選擇性鞏固（睡眠)**：20 事實,6 個 hot 反覆出現
×3、14 個 cold 一次。門控只鞏固 count≥2 的 hot：

| 指標 | 結果 |
| :-- | :-- |
| 檢索 recall（全部,deployed) | **1.00**（零遺忘) |
| 無檢索 durable recall — HOT（已鞏固) | 0.00 → **0.83**（編譯進權重、不需檢索就會) |
| 無檢索 durable recall — COLD（未鞏固) | 0.00 → 0.00（留檢索即可) |
| 通用能力（侵蝕) | 1.00 → 0.92（只鞏固 6 條,侵蝕小) |

**這就是大腦分工的端到端工程版**:海馬迴（檢索)記全部、即時、零遺忘;睡眠（門控鞏固)只把
反覆/重要的少數編譯進皮質（權重)、變成不需查找的技能;其餘留在情節記憶。**災難性遺忘在系統
層被結構性解決**（不靠把全部塞權重),樣本效率靠 self-replay,鞏固靠門控節制 → 侵蝕小。

**剩餘要做**：(i) 鞏固後可從記憶**淘汰已內化的 hot**（釋放 context/儲存,真正的 handoff);
(ii) 語意 embedder 取代 lexical 檢索（更真實);(iii) scale 模型/事實量;(iv) 鞏固品質再
push（hot durable 0.83→更高、侵蝕再降);(v) 程序性/技能類知識（非單一事實)的鞏固。

**Step 5（`llm_cl/consolidate_quality.py`）— 非侵蝕整合：侵蝕已解,durable 是剩餘瓶頸**：
鞏固時 CE on 事實（學新)＋ **對凍結 base 的 softmax-KD anchor**（在與測試無關的通用 prompt 池上,
把當前 token 分布錨在 base → 限制 collateral drift;base logits 快取)。這是 P9/P9b 的教訓:
蒸餾當「錨/限制副作用」,不是「引擎」(學習靠 CE)。

anchor 掃描（self-replay 變體固定、同 seed,乾淨 A/B,epochs=30）：

| anchor | durable | 通用侵蝕(drop) |
| :--: | :--: | :--: |
| 0（無錨) | 0.62 | **0.25** |
| 0.5 | 0.50 | 0.08 |
| **1.0** | 0.56 | **0.08** |
| 2.0 | 0.31 | 0.08 |

anchor=1.0 跨 epochs：20→durable 0.50 / **侵蝕 0.00**、30→0.56/0.08、60→0.44/0.08。

- **「侵蝕有界且小」= 已解**：base-KD anchor 把通用能力侵蝕**穩定壓到 ~0**（20 epochs 時 0.00、
  最多 0.08),不論訓多久都有界（vs 無錨 0.25 且隨 epochs 惡化)。這正是 point 4 最難的「non-eroding」
  要求。**蒸餾錨在 base（而非舊任務)是關鍵:它限制副作用、不凍結學習。**
- **「新技能單調增長到高 durable」= 尚未,瓶頸在鞏固品質**：durable 在 0.5B 上封頂 ~0.5–0.56,
  **更多 epochs 不漲（甚至略降)**;QA-format 變體（`--qa`)只小升 durable 0.50→0.56 卻把侵蝕推回
  0.17（且 0.5B 生成的 QA 良率低,16×4 只留 15)。瓶頸是**小模型的變體生成品質 + LoRA 整合容量**,
  不是再多調 anchor/epoch 能解。
- **下一個槓桿是 scale（1.5B+）**：更大模型 → 更好的自我變體 + 更高整合容量,才可能把 durable 推高;
  anchor 已把侵蝕這一軸鎖死,scale 攻 durable 那一軸。

**結論**：point 4 的「侵蝕有界且小」用 base-KD anchor 達成（侵蝕→~0、穩定);「durable 單調升高」
受限於 0.5B 的鞏固品質,需 scale。erosion 與 durable 是兩個可分開攻的軸:anchor 鎖 erosion、
scale/變體品質 攻 durable。

## 6. 環境

- `.venv/bin/python`：torch 2.12.1+cu130、transformers 5.12.1、peft 0.19.1、accelerate 1.14.0
  （RTX 2070, 8GB）。小模型 fp16 + LoRA 可訓。
